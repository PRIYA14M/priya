#include <stdio.h>
#include <string.h>
#include <conio.h>
 
int main()
{
   char i[100];
  
   printf("Enter a string to reverse\n");
   gets(i);
   strrev()  
   strrev(i);
   printf("Reversed string is: %s\n", i);
   getch();
   return 0;
}